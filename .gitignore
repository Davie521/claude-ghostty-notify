.DS_Store
*.log
*.swp
.idea/
.vscode/

# Native watcher build output. The binary is built locally by
# scripts/build-watcher.sh and never committed — hooks fall back to the bash
# watch loop wherever it is absent.
watcher/.build/
hooks/bin/
